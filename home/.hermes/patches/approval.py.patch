--- tools/approval.py	2026-06-10 20:48:38.000000000 +0100
+++ tools/approval.py	2026-06-11 04:28:38.350278760 +0100
@@ -491,6 +491,14 @@
     # into a single -X token. Catches the same threat class.
     (r'\bsudo\b[^;|&\n]*?\s+-[a-z]*[sa][a-z]*\b',
      "sudo with combined-flag privilege escalation"),
+
+    # Email send operations — hard gated: requires Telegram approval popup
+    (r'python[23]?.*/google_api\.py\b.*\bgmail (send|reply)\b', "send/reply email via Gmail (requires approval)"),
+    (r'python3.*mail_api\.py\b.*\b(send|reply)\b', "send/reply email via Microsoft Outlook (requires approval)"),
+    # WhatsApp messaging
+    (r'node.*(?:baileys\.js\b.*--send|whatsapp-send(?:-image)?\.js)\b', "send message via WhatsApp (requires approval)"),
+    # Print operations — hard gated: requires Telegram approval popup
+    (r'\blp\b.*-d\s+\bBrother-MFC\b', "print document to Brother MFC-L3740CDWE (requires approval)"),
 ]
 
 
